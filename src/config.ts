// Sentinel Frontend Configuration
// Uses Astro's PUBLIC_ env vars (available at build time)
// Export API base URL. We use an empty string so requests hit our local Astro API proxy route (/api/...)
export const API_BASE = '';
